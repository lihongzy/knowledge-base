# Build a static knowledge base site for GitHub Pages

The repository will contain a Next.js site under `site/` that reads the complete `notes/` tree only during build and emits a static site for GitHub Pages. This keeps Markdown notes as the authoritative source without requiring a server or allowing the presentation layer to modify note files.
